import { styled } from "@mui/material"


export const MyFooter = styled('footer')(() => ({
    padding: '30px',
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center'
}))

