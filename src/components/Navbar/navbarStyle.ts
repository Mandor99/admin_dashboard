/* eslint-disable @typescript-eslint/no-unused-vars */
import { styled } from '@mui/material/styles'

export const LogoBox = styled('figure')(({theme}) => ({
    width: '50px',
}))
export const LogoImg = styled('img')(({theme}) => ({
    width:'100%',
    height: '100%',
    objectFit:'cover'
}))

